# =============================================================================
# EduTrack Hub - Environment Variables
# Database: Neon (Serverless PostgreSQL)
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Required)
# -----------------------------------------------------------------------------
# Get this from Neon Console → Your Project → Connection Details → Connection string
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL="postgresql://username:password@ep-xxxxx.us-east-2.aws.neon.tech/edutrack?sslmode=require"

# -----------------------------------------------------------------------------
# AUTHENTICATION (Required)
# -----------------------------------------------------------------------------
# Generate a secure random string (minimum 32 characters)
# Run: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-minimum-32-characters"

# -----------------------------------------------------------------------------
# APPLICATION (Optional)
# -----------------------------------------------------------------------------
# Your application URL (used for redirects, emails, etc.)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node environment
NODE_ENV="development"

# -----------------------------------------------------------------------------
# PHASE 2 - PAYMENTS (Coming Soon)
# -----------------------------------------------------------------------------
# Stripe API keys (leave empty for now)
# STRIPE_SECRET_KEY=""
# STRIPE_PUBLISHABLE_KEY=""
# STRIPE_WEBHOOK_SECRET=""

# -----------------------------------------------------------------------------
# PHASE 2 - EMAIL (Coming Soon)
# -----------------------------------------------------------------------------
# Resend API key for transactional emails
# RESEND_API_KEY=""
# EMAIL_FROM="noreply@yourdomain.com"

# -----------------------------------------------------------------------------
# SETUP INSTRUCTIONS
# -----------------------------------------------------------------------------
# 1. Create a Neon account at https://neon.tech
# 2. Create a new project called "edutrack"
# 3. Copy the connection string from the dashboard
# 4. Generate JWT_SECRET: openssl rand -base64 32
# 5. Rename this file to .env.local
# 6. Run: npx prisma generate
# 7. Run: npx prisma db push
# 8. Run: npm run db:seed
# 9. Run: npm run dev
